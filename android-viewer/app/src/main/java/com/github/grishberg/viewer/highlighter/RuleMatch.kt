package com.github.grishberg.viewer.highlighter

/**
 * Helper object to keep reference to the properties of a matching segment.
 * Copied from KodeHighlighter (MIT license).
 */
data class RuleMatch(val startIndex: Int, val endIndex: Int)
