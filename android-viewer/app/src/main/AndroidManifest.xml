<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.github.grishberg.cad3d"
    android:versionCode="1"
    android:versionName="1.0.0">

    <!-- We require OpenGL ES 2.0 -->
    <uses-feature
        android:glEsVersion="0x00020000"
        android:required="true" />

    <application
        android:icon="@drawable/icon"
        android:label="@string/app_name"
        android:largeHeap="true">

        <!--
               Don't override application title. See http://stackoverflow.com/questions/3462431/how-to-name-android-application
              We will set this in code instead.
        -->
        <activity
            android:name="com.github.grishberg.viewer.Cad3dActivity"
            android:exported="true"
            android:label="@string/cad3d"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
